package com.example.a90phase.presentation.theme

import androidx.compose.ui.graphics.Color

object SleepColors {
    // Backgrounds
    val DeepSpace = Color(0xFF080E1A)
    val NavyBlue = Color(0xFF0B1120)
    val MidnightBlue = Color(0xFF131D2E)
    val GlassSurface = Color(0xFF1A2640)

    // Primary accent
    val CyanGlow = Color(0xFF00B8D9)
    val CyanSoft = Color(0xFF0096B3)

    // Nebula palette
    val NebulaViolet = Color(0xFF7C3AED)
    val NebulaPurple = Color(0xFF4C1D95)
    val NebulaDeep = Color(0xFF1E1040)

    // Discovery Phase accent
    val IndigoGlow = Color(0xFF6366F1)

    // Text
    val White = Color(0xFFFFFFFF)
    val Silver = Color(0xFFB0BEC5)
    val SlateBlue = Color(0xFF4A5568)

    // Semantic — use via SleepColors.* directly, NOT mapped to M3 slots
    val OptimalGreen = Color(0xFF10B981)
    val GoodAmber = Color(0xFFF59E0B)
    val MinimalSlate = Color(0xFF64748B)
    val PassedGray = Color(0xFF374151)
    val ErrorRed = Color(0xFFEF4444)
}
